[build]
  command = "npm run build"
  publish = "."

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = "netlify/functions"

# توجيه مريح للـ API من المتصفح
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
